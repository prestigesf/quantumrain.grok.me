# PrestigeSF Control Plane

**PACK IN → REAL ENGINE → DELTA**

Grok app for `quantumrain.grok.me`. Drop a law pack. Intercept. Read the delta.

```bash
npm install
npm run dev
```

Vite on `0.0.0.0:8080` so Grok preview can attach.
